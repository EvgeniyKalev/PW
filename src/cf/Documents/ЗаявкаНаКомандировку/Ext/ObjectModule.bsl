
Процедура ОбработкаПроведения(Отказ, РежимПроведения) 
	Если Бюджет < СтоимостьПроезда + СтоимостьПроживания Тогда
		Отказ = Истина; 
		Сообщить(СтрШаблон("Превышение бюджета на %1 рублей", СтоимостьПроезда + СтоимостьПроживания - Бюджет));
	Иначе	
		РегистрыСведений.СогласованиеКомандировок.ЗаписатьДанные(Ссылка);
	КонецЕсли;
КонецПроцедуры


Процедура ПриЗаписи(Отказ)
	// Вставить содержимое обработчика.
КонецПроцедуры

